# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# ── Lexiora ────────────────────────────────────────────────────────────────
# Drift/build_runner generated sources — regenerated via
# `dart run build_runner build` (and in CI). Not committed.
*.g.dart
# The Gradle wrapper jar is regenerated in CI (see .github/workflows/build.yml)
# so the repository stays free of binary blobs.
/android/gradle/wrapper/gradle-wrapper.jar

# Local source archive (created on request) — not part of the repo.
/lexiora-v0.10.0-source.zip

# Buffy working tooling (dedup/merge scripts, candidate batches) — not part of the repo.
/.buffy_tmp/
